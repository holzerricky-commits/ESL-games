/** Phase 4b — unified spread slide duration (ms). */
export const SPREAD_TURN_SLIDE_MS = 260

/** Slide distance as a fraction of spread width (1 = full width). */
export const SPREAD_TURN_SLIDE_DISTANCE = 1

/** Forward (+1) or backward (-1) page turn. */
export type SpreadTurnDirection = 1 | -1
