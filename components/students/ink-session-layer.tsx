/**
 * Shared ink session surface (spread ink + lesson board).
 * `BookSpreadSessionLayer` is the legacy name kept for existing imports.
 */
export {
  BookSpreadSessionLayer,
  BookSpreadSessionLayer as InkSessionLayer,
} from '@/components/students/book-spread-session-layer'
